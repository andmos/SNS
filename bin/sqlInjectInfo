#! /bin/bash

clear 

echo "The site `curl -s checkip.dyndns.com | grep -Eo "[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+"`:8080/bank is login to a online banking site."
echo "The site is poorly written, and the login goes directly against an MySQL database."
echo "Try if you can SQL Inject it do get access to a users bank balance!" 
echo "HINT: Users thomas is a good start." 
